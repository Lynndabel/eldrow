{
  "name": "base-word-daily",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:base-sepolia": "hardhat run scripts/deployBaseWordDaily.js --network baseSepolia",
    "deploy:base": "hardhat run scripts/deployBaseWordDaily.js --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0"
  }
}
